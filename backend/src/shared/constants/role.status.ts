export enum DoctorRequestStatus {
    PENDING = "PENDING",
    APPROVED = "APPROVED",
    REJECTED = "REJECTED"
}

export enum DoctorStatus {
    ACTIVE = "ACTIVE",
    DEACTIVE = " DEACTIVE"
}

export enum PatientStatus {
    ACTIVE = "ACTIVE",
    DEACTIVE = "DEACTIVE"
}